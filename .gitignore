echo -e '*.ovpn\n*.key\n*.pem\n' >> .gitignore
git add .gitignore
git commit -m "Add secrets to .gitignore"
git push
ovpn/*.ovpn
ovpn/*
!ovpn/.keep

# Ignore all files in ovpn/
ovpn/*

# Allow exceptions
!ovpn/.keep
!ovpn/README.md
